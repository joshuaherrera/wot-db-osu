/*uses jquery to set id's of nation and allegiance*/
function selectNation (id) {
	$("#nationSelector").val(id);
}
function selectFaction (id) {
	$("#factionSelector").val(id);
}
function selectPerson (id) {
	$("#personSelector").val(id);
}
